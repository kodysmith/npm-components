{
  "name": "component-core-bundler",
  "version": "1.0.0",
  "description": "core bundler component, including models, views, and css, less, sass",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "bundler",
    "view"
  ],
  "author": "Kody Smith",
  "license": "ISC",
  "bin": {
    "module-name": "./component-compiler"
  },
  "scripts": {
    "start": "echo 'usage: node component-compiler install'",
    "predeploy": "echo 'usage: node component-compiler install'",
    "postdeploy": ". build.sh",
    "prepublish": "echo 'usage: node component-compiler install'"
  },
  "dependencies": {
    "sys":  "0.0.1"
  }
}
